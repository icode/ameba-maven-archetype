[main]
##########################################################
#The following Base64 string was generated by auto-generating an AES Key:
#       AesCipherService aes = new AesCipherService();
#       byte[] key = aes.generateNewKey().getEncoded();
#       // jdk 1.8
#       String base64 = Base64.encodeToString(key);
#       // or all jdk use Guava
#       String base64 = BaseEncoding.base64().encode(key);
# todo: use up code generating an AES Key
#securityManager.rememberMeManager.cipherKey=4AvVhmFLUs0KTA3Kprsdag==


# the maximum age of the `remember me` cookie in seconds. default 7 days
#securityManager.rememberMeManager.cookie.maxAge=10
#cacheManager=ameba.security.shiro.cache.DefaultCacheManager
#realm.cacheManager=$cacheManager